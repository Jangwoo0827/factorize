# FACTORIZE — 기획/설계 문서 (v2.2)

> **게임 타이틀: FACTORIZE**
> "모든 것을 공장화한다" — 채굴부터 가공, 판매, 연구까지 세상 모든 과정을 자동화 공정으로 만든다는 컨셉을 담은 이름.
>
> 상태: **설계 단계** (코드 작성 전)
> 다음 단계 진입 조건: 사용자가 "다음"이라고 입력

## CHANGELOG

### v2.2 (다중 선택 채굴 자원 일괄 변경)
- **[추가]** 다중 선택으로 채굴기 여러 개를 고르면, 우측 패널에 "채굴 자원 일괄 변경" 버튼들이 뜬다. 등급이 섞여 있으면 선택된 채굴기 전부가 캘 수 있는 자원(=하위 등급이 캘 수 있는 자원은 상위 등급도 항상 캘 수 있으므로, 결국 가장 낮은 등급의 목록)만 보여주고, 버튼을 누르면 전부 그 자원으로 즉시 바뀐다.
- `Miner`에 `cycleResource()`(M 키, 한 칸씩 순환)와 별개로 `setResource(resourceType)`(원하는 자원으로 직행, 불가능하면 false)를 추가.

### v2.1 (미니맵)
- **[완료]** 미니맵 (6.8) — 우하단에 항상 표시되는 160×160 미니맵. 배치된 모든 건물을 각자의 건물 색상 점으로 찍고, 원점 마커와 현재 메인 카메라가 보는 영역을 사각형으로 표시. 건물이 거의 없을 때 과도하게 확대되지 않도록 최소 표시 반경(40타일)을 보장. 클릭하면 그 위치로 카메라가 부드럽게 이동(`Camera.panTo()` 추가, 기존 target/보간 구조를 그대로 재사용).
- `Renderer.js`에 `MinimapRenderer` 클래스 추가 (메인 `Renderer`와 파일 공유, 별도의 작은 `<canvas>`를 직접 그림).

### v2.0 (마일스톤 + 업적 + 밸런스 수정)
- **[완료]** 마일스톤 (6.6) — "누적 수익"/"초당 수익 최고 기록" 두 사다리(각 5단계). 별도 해금 상태 없이 `world.stats.totalRevenue`/`peakIncomeRate`(항상 증가만 함)를 기준으로 매번 다시 계산.
- **[완료]** 업적 시스템 — 12종(판매/건물 수/연구/제작/업그레이드 횟수 기준). `AchievementSystem`이 `CONFIG.ACHIEVEMENTS`의 `type`+`target`을 매 틱 판정하고, 새로 달성하면 미니로그에 알림. 해금 전에는 이름/설명을 "???"로 가림. "달성 현황" 토글 버튼으로 마일스톤+업적을 한 패널에서 확인.
- **[밸런스 수정]** 업그레이드 비용을 전 건물 공통 고정값(50/150/400원)에서 건물 자신의 배치 비용에 비례한 배율(×2.5/×6/×15)로 변경 — 다중 선택 일괄 업그레이드가 생기면서 "제일 싼 건물부터 업그레이드"가 사실상 강제 최적 전략이 되어버린 문제를 해결.
- `world.stats`(ProductionStats)에 누적(lifetime) 필드(총 수익/총 판매/자원별 총 생산/총 업그레이드 횟수)를 추가하고 세이브 대상에 포함시킴 — 예전엔 순간 수치라 저장하지 않았지만, 마일스톤/업적이 세션을 넘어 유지되려면 필요해짐.

### v1.9 (컨베이어/발전 등급 + 건물 UI 탭)
- **[완료]** 연구 해금형 컨베이어 등급 (6.2) — 컨베이어 II/III 추가(2.5, 4.0타일/초). 채굴기와 동일한 "연구로 상위 등급 해금" 패턴을 그대로 적용.
- **[완료]** 연구 해금형 발전기 등급 (6.4의 "다중 발전원" 대신 채택) — 발전기 II/III 추가(초당 55/120 공급). 여러 연료 타입 대신, 이미 정한 방향대로 채굴기/컨베이어와 같은 "등급업" 패턴으로 통일.
- **[추가]** 건물 목록 UI에 카테고리 탭 도입(채굴/생산·물류·전력·기타) — 마인크래프트 창작 모드 인벤토리 참고. 건물이 20종에 가까워지면서 한 번에 다 보여주기보다 탭으로 나누는 게 나음. `CONFIG.BUILD_CATEGORIES`에 탭을 정의하고 각 `BUILDINGS` 항목에 `category`를 태그하는 방식이라, 새 건물 추가 시 탭 관련 코드는 건드릴 필요 없음. 해체/블루프린트/다중 선택 도구는 건물이 아니므로 모든 탭에서 항상 보임.
- `Conveyor`/`Generator` 클래스를 `Miner`와 같은 패턴(typeId 파라미터화 + 티어별 thin 서브클래스)으로 리팩터링.

### v1.8 (콘텐츠 확장: 신규 광물 제련/가공/조립 체인)
- **[완료]** 금/티타늄 제련 레시피 — 용광로에서 금광석→금괴(4초), 티타늄광석→티타늄괴(5초, 정련이 까다롭다는 설정으로 최장). 희귀 결정은 이미 정제된 자원이라는 설정으로 제련/가공 없이 조립에 바로 사용.
- **[완료]** 금/티타늄 가공 레시피 — 분쇄기(→금가루/티타늄가루), 세척기(→세척 금광석/세척 티타늄광석)에 기존 철/구리와 같은 패턴으로 추가.
- **[완료]** 티어를 넘나드는 조립 레시피 3종 — 합금 프레임(티타늄괴+철괴), 정밀 회로(금괴+기존 회로, 조립품을 다시 조립 재료로 사용), 동력 코어(희귀 결정+티타늄괴+모터, 채굴기 III까지 요구하는 최종 조립품).
- 전부 `config.js`(RECIPES/PROCESSING_RECIPES/ASSEMBLY_RECIPES/SELL_PRICES)와 `Resources.js` 데이터 추가만으로 구현됨 — Furnace/Processor/Assembler 로직은 이미 완전히 데이터 기반이라 코드 변경 없음.

### v1.7 (콘텐츠 확장: 채굴기 등급 + 다중 선택)
- **[완료]** 연구 해금형 채굴기 등급 (6.1) — 채굴기 II/III 추가. 상위 등급은 연구(각각 `tech_miner_t2`/`tech_miner_t3`)로 해금되고, 하위 등급이 캐던 자원을 모두 포함해 금광석/티타늄광석(II), 희귀 결정(III)까지 캘 수 있음. 채굴 속도(1.5s/1.0s)와 전력 소모(8/14)도 등급별로 다름. `Miner` 클래스를 typeId 파라미터화해서 `Crusher`/`Washer`가 `Processor`를 공유하는 것과 같은 패턴으로 `MinerT1/T2/T3`가 공유하도록 리팩터링. 새 광물의 제련/가공 레시피는 아직 없음(직접 판매만 가능) — 후속 확장 여지로 남겨둠.
- **[추가]** 다중 선택 + 일괄 업그레이드 — "다중 선택" 도구로 드래그하면 영역 안 건물을 모두 골라 우측 패널에 종류별 개수로 보여주고(가장 많은 종류를 대표로 제목에 표시), 업그레이드 버튼 한 번으로 전부 업그레이드 시도. 자금이 모자라면 살 수 있는 만큼만 처리(블루프린트 붙여넣기와 같은 철학). 블루프린트 도구가 쓰던 드래그-선택 입력 처리를 그대로 재사용.

### v1.6 (구현 완료 + UI 개편)
- **[완료]** 블루프린트 복사/붙여넣기 (6.2) — "블루프린트" 도구로 좌클릭 드래그해 영역을 복사(종류+방향만, 진행 상태는 담지 않음), 이후 클릭할 때마다 도장 찍듯 붙여넣기. R로 전체 회전. 개별 칸이 막혀있거나 자금이 부족하면 그 칸만 건너뛰고 나머지는 배치. 0순위 3항목(초당 생산량/생산 그래프/블루프린트) 전부 완료.
- **[변경]** 건물 단축키(숫자/Q/W/E/A) 전부 제거 — WASD 카메라 이동과 겹쳐 불편하다는 피드백. 건물 선택은 좌측 드로어 버튼으로만.
- **[변경]** 좌측 건물 패널을 상시 노출 대신 "건설" 토글 버튼으로 여닫는 슬라이드 드로어로 변경.
- **[변경]** 항상 떠 있던 조작 안내(controls-hint)를 제거하고, 확장 가능한 설정(Settings) UI로 이전 — 향후 실제 설정 항목(그래픽/사운드 등) 추가를 염두에 두고 섹션/토글 빌더를 범용으로 설계.

### v1.5 (구현 완료)
- **[완료]** 초당 생산량 표시 (6.8) — `Systems.js`에 `ProductionStats` 신설(생산/판매 이벤트를 1초 창으로 집계), 하단 상태바에 "생산 X/s", "수익 +X/s" 추가.
- **[완료]** 생산 통계 그래프 (6.8) — `ProductionStats.productionHistory`(자원별 최근 90초 이력)를 재사용해 `UIManager.js`의 `ProductionGraphPanel`이 자원별 SVG 스파크라인을 그림. "생산 그래프" 토글 버튼(index.html)으로 열고 닫음.
- 남은 0순위 항목: 블루프린트 복사/붙여넣기 (6.2) — 아직 착수 전.

### v1.4 (수정)
- **[반려]** 매장지(vein)/순도 시스템(v1.3의 6.1) — 사용자 피드백: 공간에 매장량을 얹으면 플레이가 혼란스러워짐. 채택하지 않음.
- **[채택]** 대안: **연구 해금형 채굴기 등급 시스템** — 채굴기를 업그레이드(연구)하면 더 다양한/희귀한 광물을 캘 수 있게 하는 방식으로 6.1을 대체. 위치 기반 복잡도 없이 "연구 진행 → 새 광물 접근"이라는 기존 연구 시스템(섹션 2.5)의 흐름 그대로 확장.
- **[확정]** 다음 구현 대상 3종 확정: 생산 통계 그래프(6.8), 초당 생산량 표시(6.8), 블루프린트 복사/붙여넣기(6.2). 착수 순서는 별도 논의.

### v1.3 (추가)
- **[추가]** 섹션 6 "콘텐츠 확장 아이디어 (타 공장 게임 벤치마킹)" 신설
  - Phase 0~9(엔진 뼈대~성능 최적화) 완료 후, Phase 10 콘텐츠 확장 단계에서 참고할 아이디어 뱅크
  - Factorio / Satisfactory / Dyson Sphere Program / Shapez.io / Mindustry의 핵심 시스템을 카테고리별로 정리하고, 현재 아키텍처(2D 타일/청크, DOM UI, 전력망, 기술트리, 경제) 기준 도입 난이도로 우선순위 제안
  - 아직 "설계 아이디어" 단계이며 실제 채택/구현 계획은 각 항목 착수 시 별도 확정

### v1.2 (수정)
- **[변경]** 게임 타이틀 확정: **FACTORIZE**
  - 의미: "모든 것을 공장화한다(Factorize everything)" — 채굴/가공/판매/연구 전 과정을 자동 생산 라인으로 전환한다는 핵심 컨셉과 직결되는 이름
  - index.html의 `<title>`, HTML 상단 로고 텍스트, package/README 등에 반영 예정 (Phase 0 구현 시 적용)

### v1.1 (수정)
- **[변경]** 품질 목표 명시: Pygame 프로토타입 수준을 상회하는 완성도 목표 추가 (섹션 1.1)
  - Canvas 렌더링 디테일(그라디언트/그림자/보간 애니메이션), DOM 기반 UI 품질 강조
- **[변경]** 폴더 구조 전면 재설계: 40개+ 파일 → **17개 파일**로 통합 (섹션 3)
  - "클래스 1개 = 파일 1개" 원칙을 "클래스 단위 SRP 유지 + 연관 클래스는 파일 공유"로 완화
  - `Building.js`, `Systems.js`, `UIManager.js` 등에 다수 클래스를 그룹화
- **[변경]** 클래스 다이어그램에 파일 매핑 설명 주석 추가 (섹션 4)
- 클래스 간 관계/책임 분리 자체는 v1.0과 동일 (구조 변경 없음, 파일 배치만 변경)

### v1.0 (최초 작성)
- 최초 기획서/설계/폴더 구조/클래스 다이어그램/로드맵 작성

---

## 1. 전체 게임 기획서

### 1.1 게임 컨셉
- **타이틀: FACTORIZE**
- 장르: 2D 타일 기반 자동화 공장 시뮬레이션
- 참고작: Factorio, shapez.io — 단, 그대로 복제하지 않고 **우리만의 룰셋**을 가진 독립 게임으로 설계
- 플랫폼: 웹 브라우저 (GitHub Pages / itch.io 배포 목표)
- 기술: HTML5 + CSS3 + Vanilla JS(ES6) + Canvas API, 프레임워크 미사용
- **품질 목표**: Pygame으로 만든 프로토타입 수준을 상회하는 완성도를 목표로 한다.
  - Canvas의 안티에일리어싱, `globalAlpha`, 그라디언트, 그림자(shadowBlur), 부드러운 easing 애니메이션을 적극 활용해 "도형만 쓰지만 조잡하지 않은" 비주얼을 구현한다.
  - UI는 Canvas가 아닌 **DOM(HTML/CSS)**으로 구성해 폰트 렌더링/레이아웃/반응형 품질을 Pygame 기본 UI보다 확실히 높인다.
  - 카메라 이동/줌은 항상 보간(interpolation) 처리하여 끊김 없는 움직임을 제공한다.
  - 이 목표는 코드 구조가 아니라 렌더링/UX 디테일에 관한 것이므로, 아래 파일 수 제약과는 별개로 항상 지켜야 하는 원칙이다.

### 1.2 핵심 게임 루프
```
채굴(광석 확보) → 운송(컨베이어) → 가공(제련/제작) → 판매(수익) 
    → 재투자(건물 구매/업그레이드) → 연구(신기술 해금) → 더 큰 생산 라인
```
이 루프가 반복되며 플레이어는 점점 더 복잡하고 큰 규모의 공장을 설계하게 된다.

### 1.3 핵심 재미 요소
1. **공간 퍼즐**: 제한된 타일 위에 컨베이어/기계를 효율적으로 배치
2. **처리량 최적화**: 병목 현상을 관찰하고 해소하는 재미
3. **점진적 확장**: 작은 라인 → 대규모 자동화 단지로 성장하는 성취감
4. **전력 관리 리스크**: 전력이 부족하면 전체 라인이 느려지는 긴장감
5. **연구를 통한 해금 구조**: 새로운 건물/광물/레시피가 열리며 플레이 패턴이 계속 갱신됨

### 1.4 초기 콘텐츠 범위 (MVP)
| 카테고리 | 항목 |
|---|---|
| 자원 | 철광석, 구리광석, 석탄 |
| 가공물 | 철괴, 구리괴 |
| 건물 | 채굴기, 컨베이어, 용광로, 판매기, 창고, 발전기, 전선, 연구소 |
| 엔티티 | 플레이어(카메라 겸용 커서), 아이템 객체 |
| 시스템 | 자동화(운송/가공), 전력, 연구, 경제, 저장(세이브) |

MVP 이후 광물/건물/레시피/UI 확장은 로드맵의 후속 단계에서 순차적으로 진행한다.

### 1.5 목표 사용자 경험
- 처음 5분: 손으로 직접 채굴 → 컨베이어를 깔아보며 자동화의 손맛을 느낌
- 처음 30분: 전력 시스템의 제약을 이해하고 배치를 고민하기 시작
- 1시간+: 연구를 통해 새로운 건물을 해금하고, 공장을 재설계

---

## 2. 전체 시스템 설계

### 2.1 게임 루프 아키텍처
```
main.js
  └─ Game (게임 전체 상태를 소유하는 루트 클래스)
       ├─ Loop (requestAnimationFrame 기반 fixed-timestep 루프)
       ├─ World (타일맵, 청크, 엔티티 컨테이너)
       ├─ Camera (이동/줌/드래그)
       ├─ InputManager (마우스/키보드 입력 중앙 처리)
       ├─ Renderer (Canvas 렌더링 파이프라인)
       ├─ SystemManager (모든 System을 등록/실행)
       ├─ UIManager (모든 UI 패널 관리)
       └─ SaveManager (localStorage 직렬화/역직렬화)
```

**설계 원칙**
- Game은 "조립자" 역할만 한다. 실제 로직은 각 System과 Entity 클래스에 위임한다 (SRP).
- 모든 게임 로직은 `update(dt)` 사이클을 기준으로 동작하는 **fixed timestep**을 사용해 시뮬레이션 정확도를 보장한다. 렌더링은 별도로 interpolation 가능하도록 분리한다.
- 엔티티(건물)와 시스템(로직)을 분리하는 **ECS에 가까운 하이브리드 구조**를 사용한다. 다만 완전한 ECS는 오버엔지니어링이므로, "클래스 기반 엔티티 + 시스템이 순회하며 처리" 방식의 절충안을 택한다.

### 2.2 월드 & 타일 시스템
- 기본 타일 크기: 64x64px (상수 `TILE_SIZE`로 관리)
- 월드는 **청크(Chunk) 단위**로 분할 관리 (예: 16x16 타일 = 1청크)
  - 이유: 맵이 매우 커질 수 있으므로 전체 타일 배열을 매 프레임 순회하면 성능이 무너짐
  - 카메라 뷰포트에 걸친 청크만 렌더링/업데이트 대상으로 선정 (Frustum Culling)
- 타일 좌표계: `worldX, worldY` (픽셀) ↔ `tileX, tileY` (정수) ↔ `chunkX, chunkY` 변환 유틸 제공

### 2.3 자동화(컨베이어) 시스템
- 아이템은 독립된 객체(`Item`)이며, 컨베이어 위에서 "차지하고 있는 비율(progress 0~1)"을 가진다.
- 컨베이어는 자신의 진행 방향과 다음 컨베이어(혹은 기계 입력구)에 대한 참조를 가진다.
- 매 틱마다:
  1. 각 컨베이어는 자신 위의 아이템들을 이동시킨다.
  2. progress가 1.0에 도달하면 다음 컨베이어/기계로 전달을 시도한다.
  3. 다음 슬롯이 막혀 있으면 전달을 보류 → **자연스러운 병목현상** 발생.
- 성능을 위해 컨베이어 자체를 그리드 기반 배열로 관리하고, "빈 컨베이어는 업데이트 스킵" 최적화를 적용한다.

### 2.4 전력 시스템
- 발전기(PowerGenerator)는 초당 일정 전력을 생산한다.
- 전선(PowerWire)으로 연결된 건물들은 하나의 **전력망(PowerGrid)**을 구성한다 (Union-Find 또는 그래프 탐색으로 그룹화).
- 매 틱마다 PowerGrid는 (총 발전량 vs 총 요구량)을 계산하여 `supplyRatio(0~1)`을 산출한다.
- 각 기계는 `supplyRatio`에 비례하여 작업 속도가 감소한다 (완전 정지가 아닌 점진적 감속으로 설계하여 디버깅 편의성과 재미를 둘 다 확보).

### 2.5 연구 시스템
- 연구소(Lab)가 연구 포인트(RP)를 소비하여 `TechTree`의 노드를 해금한다.
- 각 기술 노드는 선행 조건(prerequisite)을 가진 그래프 구조.
- 해금 시 `UnlockRegistry`에 새로운 건물/레시피/광물이 등록되고, 건물 선택 UI에 자동 반영된다.

### 2.6 경제 시스템
- 판매기(Seller)에 도달한 아이템은 자동으로 판매되어 골드 획득.
- 골드는 건물 구매, 연구 가속, 업그레이드에 사용.
- 가격 테이블은 하드코딩하지 않고 `config/economyConfig.js`에서 관리 (확장/밸런싱 용이).

### 2.7 UI 레이아웃
```
┌─────────────────────────────────────────┐
│  상단: 연구 진행도 바                      │
├───────┬───────────────────────┬─────────┤
│ 좌측   │                       │  우측    │
│ 건물   │      게임 캔버스        │  선택된  │
│ 선택   │                       │  건물    │
│ 패널   │                       │  정보    │
├───────┴───────────────────────┴─────────┤
│  하단: 자원 / 돈 / 전력 / FPS / 미니로그   │
└─────────────────────────────────────────┘
```
- UI는 DOM 오버레이(HTML/CSS) + Canvas 게임 화면을 분리하는 방식을 채택 (Canvas 안에 텍스트 UI를 그리는 것보다 유지보수/접근성이 훨씬 좋음).

### 2.8 저장(Save) 시스템
- `SaveManager`가 `World`, `Economy`, `TechTree`, `PowerGrid` 등 각 시스템의 `serialize()/deserialize()`를 호출해 하나의 JSON으로 직렬화 후 localStorage에 저장.
- 자동 저장(주기적) + 수동 저장(버튼) + 불러오기 지원.
- 버전 필드(`saveVersion`)를 포함시켜 추후 세이브 마이그레이션에 대비.

### 2.9 성능 전략
- 청크 기반 컬링으로 화면 밖 객체 업데이트/렌더링 최소화
- 아이템 객체 풀링 (`ItemPool`) — 생성/파괴 GC 부담 감소
- 공간 분할이 필요해지면 QuadTree를 `world/spatial/` 모듈로 추가 가능하도록 인터페이스 예약
- 고정 타임스텝 시뮬레이션 + 렌더 보간으로 60FPS 목표

---

## 3. 폴더 구조 (수정: 최대 20개 파일 이하로 통합)

> **변경 사유**: 원래 안은 "클래스 1개 = 파일 1개"를 엄격히 지켜 40개 이상의 파일이 필요했다. 요청에 따라 **총 파일 수를 20개 이하**로 제한한다. 대신 "클래스 1개 = 파일 1개" 원칙은 **"클래스는 여전히 역할별로 명확히 분리하되, 강하게 연관된 클래스들을 같은 파일(모듈) 안에 묶는다"**로 완화한다. 즉 SRP는 **클래스 단위**에서는 그대로 유지하고, 파일 단위에서는 "관련 클래스 그룹"으로 묶는 절충을 취한다.

```
factory-game/                    (총 17개 파일)
├─ index.html                     [1]
├─ style.css                      [2]
│
├─ config.js                      [3]  # 전역 상수 + 건물/레시피/경제/연구 설정 전체 통합
│                                       # (GAME, TILE, BUILDINGS, RECIPES, ECONOMY, TECH_TREE)
│
├─ src/
│  ├─ main.js                     [4]  # 진입점: Game 인스턴스 생성/시작만 담당
│  │
│  ├─ core/
│  │  ├─ Game.js                  [5]  # 최상위 게임 클래스 (조립자)
│  │  ├─ Loop.js                  [6]  # 고정 타임스텝 루프
│  │  ├─ Camera.js                [7]  # 카메라 이동/줌/드래그(보간 포함)
│  │  └─ InputManager.js          [8]  # 입력 중앙 처리
│  │
│  ├─ world/
│  │  └─ World.js                 [9]  # class World, class Chunk, class Tile
│  │                                    # (좌표 변환 유틸 함수 포함, 필요 시 QuadTree도 이 파일에 추가)
│  │
│  ├─ entities/
│  │  ├─ Entity.js                [10] # class Entity(추상), class Item, class Player
│  │  └─ Building.js              [11] # class Building(추상) +
│  │                                    # Miner/Conveyor/Furnace/Seller/Storage/
│  │                                    # Generator/PowerWire/Lab (각각 독립 class, 파일만 공유)
│  │
│  ├─ resources/
│  │  └─ Resources.js             [12] # class ResourceType, class ResourceRegistry
│  │
│  ├─ systems/
│  │  └─ Systems.js               [13] # class SystemManager +
│  │                                    # ConveyorSystem / ProductionSystem / PowerSystem /
│  │                                    # EconomySystem / ResearchSystem / TechTree / UnlockRegistry
│  │
│  ├─ rendering/
│  │  └─ Renderer.js              [14] # class Renderer + TileRenderer / EntityRenderer / DebugRenderer
│  │
│  ├─ ui/
│  │  └─ UIManager.js             [15] # class UIManager +
│  │                                    # BuildMenuPanel / InspectorPanel / StatusBar / ResearchBar / MiniLog
│  │
│  ├─ save/
│  │  └─ SaveManager.js           [16] # class SaveManager, class SaveSchema
│  │
│  └─ utils/
│     └─ Utils.js                 [17] # class EventBus, class ObjectPool, class Logger, MathUtils 함수 모음
│
└─ assets/                             # 코드 파일 아님 (스프라이트/폰트 추가 시 사용, 초기엔 비어있음)
```

**파일 분리 원칙 (수정본)**
- **클래스 단위 SRP는 유지**: 한 클래스는 여전히 하나의 책임만 갖는다. 다만 서로 강하게 연관된 클래스(예: `Building`과 그 8개 하위 클래스, 5개 System과 `SystemManager`)는 **같은 파일 안에 여러 class 선언**으로 묶어 파일 수를 줄인다.
- 각 파일 상단에 해당 파일이 담고 있는 클래스 목록을 주석으로 명시해 탐색성을 확보한다.
- `config.js`는 순수 데이터 객체만 포함(로직 없음) → 밸런싱 시 다른 코드 수정 불필요.
- 파일이 지나치게 커지면(예: `Building.js`가 1000줄을 넘는 등) 그때는 예외적으로 분리 여부를 다시 논의한다. 단, 기본 원칙은 20개 이하 유지.
- 현재 구조는 17개 파일로, 향후 확장(신규 시스템 등)을 위해 3개 파일의 여유를 남겨둔다.

---

## 4. 클래스 다이어그램 (구조 개요)

> 아래 다이어그램의 클래스 관계 자체는 변경되지 않았다. 다만 실제로는 여러 클래스가 하나의 파일에 함께 선언된다 (예: `Building.js` 안에 `Building`, `Miner`, `Conveyor` 등 9개 클래스가 함께 존재). 파일 경로는 3번 섹션을 참고.

```
Game
 ├─ has-a → World
 ├─ has-a → Camera
 ├─ has-a → InputManager
 ├─ has-a → Renderer
 ├─ has-a → SystemManager
 ├─ has-a → UIManager
 └─ has-a → SaveManager

World
 ├─ has-many → Chunk
 │                └─ has-many → Tile
 └─ has-many → Entity (buildings, items, player)

Entity (abstract)
 ├─ id, position(x,y), rotation
 └─ update(dt), render(ctx)

Building extends Entity (abstract)
 ├─ powerRequirement, isPowered, workProgress
 ├─ getRecipe(), canProcess(), process(dt)
 └─ 하위: Miner, Conveyor, Furnace, Seller, Storage, Generator, PowerWire, Lab

Item extends Entity
 ├─ resourceType, progressOnConveyor
 └─ 컨베이어를 따라 이동하는 상태를 가짐

SystemManager
 └─ has-many → System (interface)
       ├─ ConveyorSystem   : Conveyor + Item을 순회하며 이동 처리
       ├─ ProductionSystem : Miner/Furnace 등 생산 로직 처리
       ├─ PowerSystem      : PowerWire로 연결된 그래프 → PowerGrid 계산
       ├─ EconomySystem    : Seller 도달 아이템 → 골드 변환
       └─ ResearchSystem   : Lab의 RP 누적 → TechTree 해금

TechTree
 └─ has-many → TechNode (id, cost, prerequisites[], unlocks[])

UIManager
 └─ has-many → UIPanel (interface: render(), onGameStateChange())
       ├─ BuildMenuPanel
       ├─ InspectorPanel
       ├─ StatusBar
       └─ ResearchBar

SaveManager
 └─ uses → SaveSchema
       (World, EconomySystem, TechTree, PowerSystem 각각의
        serialize()/deserialize() 를 호출하여 통합 JSON 구성)
```

**인터페이스 설계 포인트**
- `System` 인터페이스: 모든 시스템은 `update(dt, world)` 메서드를 갖는다 → `SystemManager`가 동일한 방식으로 순회 가능 (OCP: 새 시스템 추가 시 기존 코드 수정 불필요)
- `Building`은 추상 클래스로, 공통 로직(전력 소모, 진행도 처리)을 상위에서 구현하고 하위 클래스는 `getRecipe()`, `onProcessComplete()` 등만 override (템플릿 메서드 패턴)
- `EventBus`를 통해 시스템 간 직접 참조를 최소화 (예: EconomySystem이 골드를 획득하면 이벤트 발행 → UI가 구독하여 갱신, UI가 EconomySystem을 직접 알 필요 없음)

---

## 5. 개발 로드맵

> 각 단계는 **독립적으로 실행 가능한 상태**를 목표로 하며, 완료 후에만 다음 단계로 진행합니다.

### Phase 0 — 프로젝트 뼈대
- index.html / style.css 기본 골격
- Game, Loop, Renderer 최소 구현 → 빈 캔버스에 60FPS 루프 동작 확인
- 산출물: 검은 화면이지만 FPS 카운터가 도는 상태

### Phase 1 — 월드 & 카메라
- World, Chunk, Tile 구현
- Camera 이동/줌/드래그
- 타일 그리드를 도형(사각형)으로 렌더링
- 산출물: 무한에 가까운 타일 맵을 카메라로 탐색 가능

### Phase 2 — 엔티티 기반 & 건물 배치
- Entity, Building 베이스 클래스
- InputManager로 타일 클릭 → 건물 배치/제거
- BuildMenuPanel(좌측 UI) 최소 버전
- 채굴기, 컨베이어 우선 구현 (도형: 사각형/원)

### Phase 3 — 자동화 코어
- Item 클래스 + ConveyorSystem
- 채굴기 → 컨베이어 → 창고 흐름 완성
- 병목현상 확인 가능한 데모 라인 구성

### Phase 4 — 가공 & 경제
- Furnace(용광로), Seller(판매기) 구현
- ProductionSystem, EconomySystem
- 하단 StatusBar에 자원/돈 표시

### Phase 5 — 전력 시스템
- Generator, PowerWire 구현
- PowerSystem: 전력망 그래프 구성 + supplyRatio 계산
- 전력 부족 시 기계 감속 시각적 피드백

### Phase 6 — 연구 시스템
- Lab, TechTree, ResearchSystem
- ResearchBar(상단 UI), UnlockRegistry 연동
- 해금 시 BuildMenuPanel 자동 갱신

### Phase 7 — UI 고도화
- InspectorPanel(우측: 선택 건물 정보) 완성
- MiniLog(하단 이벤트 로그)
- 전체 UI 스타일링

### Phase 8 — 세이브 시스템
- SaveManager, SaveSchema
- 자동/수동 저장, 불러오기
- 버전 필드 및 예외 처리(손상된 세이브 대응)

### Phase 9 — 성능 최적화
- 청크 컬링 검증
- ObjectPool 적용(Item 등)
- 필요 시 QuadTree 도입
- 대규모 공장(수백~수천 개 건물) 스트레스 테스트

### Phase 10 — 콘텐츠 확장 (반복 단계)
- 신규 광물/건물/레시피 순차 추가
- 그래픽을 단순 도형에서 스프라이트로 전환 (요청 시)
- 밸런싱 조정

---

## 6. 콘텐츠 확장 아이디어 (타 공장 게임 벤치마킹)

> Phase 0~9(엔진 뼈대~성능 최적화)가 완료된 시점에서, Phase 10 "콘텐츠 확장"에 무엇을 채울지에 대한 아이디어 뱅크. Factorio, Satisfactory, Dyson Sphere Program(DSP), Shapez.io, Mindustry의 핵심 시스템 중 우리 아키텍처(2D 타일/청크 월드, DOM UI, 전력망 그래프, 기술트리, 판매 기반 경제)에 잘 붙는 것 위주로 정리한다. 전투/생존 계열(Mindustry의 방어전 등)은 이 게임의 컨셉(생산 최적화)과 맞지 않는 부분이 많아 참고만 하고 그대로 가져오지 않는다.

### 6.1 채굴/자원 — 참고: Factorio, DSP
> **[v1.4 결정]** 매장지(vein)/매장량/순도 시스템은 채택하지 않음 — 위치 기반 자원 관리가 플레이를 혼란스럽게 만든다는 피드백. 대신 아래 "연구 해금형 채굴기 등급" 방식을 채택.

- **연구 해금형 채굴기 등급 (채택)**: 채굴기 자체를 여러 등급으로 나누고, 상위 등급은 연구로 해금한다. 등급이 오를수록 캘 수 있는 광물 종류가 늘어남(예: 기본 채굴기 = 철/구리/석탄, 2등급 = +금/티타늄, 3등급 = +희귀광물). 위치나 매장량 계산 없이 기존 `TECH_TREE` → `UnlockRegistry` 흐름(섹션 2.5)에 그대로 태워서 구현 가능 — 새 건물(`BUILDINGS`에 `miner_t2` 등 추가) + 새 자원(`RESOURCES`) + 새 기술 노드 조합만으로 확장되는 구조라 구조 변경이 작음.
- **액체 자원 + 파이프라인**: 원유/물 같은 2차 자원 축. 고체(컨베이어)와 다른 운송 수단(파이프)이 필요해 물류 다양성이 생김. 다만 새 엔티티 타입과 렌더링이 추가로 필요해 규모가 큼. (참고용으로 유지, 우선순위는 낮음)

### 6.2 물류/운송 — 참고: Factorio, Shapez.io, DSP
- **컨베이어 속도 티어**: 기본 → 고속 → 초고속처럼 상위 컨베이어를 신규 건물로 추가. 기존 `PRODUCTION.CONVEYOR_SPEED` 구조를 재사용하기 쉬움.
- **지하 컨베이어(터널)**: 라인이 교차할 때 우회 배선 없이 지나갈 수 있게 함. 좁은 공간에서 배치 밀도를 크게 높여주는 QoL이자 새로운 퍼즐 요소.
- **블루프린트(복사/붙여넣기/저장) (채택)**: 반복되는 생산 유닛을 매번 손으로 다시 짓지 않아도 되게 함. 공장이 커질수록(=사용자가 원하는 "더 크게") 체감 효과가 가장 큰 항목 중 하나.
- **장거리 물류(기차 등)**: 멀리 떨어진 매장지 ↔ 본진 간 대량 운송. 매장지 시스템(6.1)이 먼저 들어가야 필요성이 생기는 후속 항목.

### 6.3 생산 체인 심화 — 참고: Factorio, DSP, Satisfactory
- **다단계 정련**: 광석 → 괴 → 합금(예: 강철) → 부품 → 완제품으로 트리를 더 깊게. 지금 `ASSEMBLY_RECIPES`(기어/모터/회로) 다음 단계를 얹는 자연스러운 확장.
- **부산물 처리**: 일부 레시피에서 부산물(폐가스, 슬래그 등)이 나오고, 방치하면 페널티 또는 처리 건물이 필요하게 함 → 후반 최적화 압박 추가.
- **순환 레시피**: 산출물 일부가 다시 원료로 재투입되는 루프(DSP의 재활용로 등). 후반 효율화 콘텐츠로 적합.

### 6.4 전력 — 참고: Factorio, Satisfactory
- **다중 발전원**: 화력(석탄) 외에 태양광·핵연료 등을 상위 기술로 해금. 각기 다른 트레이드오프(연료비 vs 초기비용 vs 안정성)를 주는 게 핵심.
- **전력 저장(배터리)**: 발전 불안정 구간을 완충. 지금의 `supplyRatio` 감속 시스템과 자연스럽게 결합 가능.
- **브라운아웃 경고 UI**: 새 시스템 없이도 `PowerSystem`의 기존 계산값을 MiniLog/StatusBar에 경고로 노출하는 것만으로 체감 개선.

### 6.5 연구 — 참고: Factorio(무한 연구), DSP
- **다중 연구 재화**: RP 외에 희귀 광물 등 특수 자원을 소모하는 고급 기술 노드.
- **무한 연구(인피니트 테크)**: 예) "채굴 속도 +1%"를 반복 연구 가능하게 하여, 트리를 다 해금한 뒤에도 장기 목표 제공.
- **기술 트리 그래프 UI**: 지금 `TECH_TREE`가 4개 노드뿐이라 리스트로 충분하지만, 노드 수가 늘어나면 관계를 보여주는 그래프형 UI가 필요해짐.

### 6.6 경제/목표 — 참고: Factorio(로켓 발사), Satisfactory(마일스톤), Shapez.io(레벨)
- **마일스톤/챕터형 목표**: "N단계 부품 M개 납품" 같은 단계별 목표를 제공해 무엇을 지어야 할지 방향을 줌. 지금은 자유도만 있고 뚜렷한 중간 목표가 없음.
- **계약/퀘스트**: 특정 완제품 조합을 기한 내 납품하면 보상(골드/RP)을 주는 반복 콘텐츠.
- **수요 기반 가격 변동**: 한 자원만 대량 판매하면 가격이 떨어지게 해 생산 다각화를 유도(선택적, 경제 시뮬레이션 성격이 강해지므로 신중히 검토).

### 6.7 위협/도전 요소 — 참고: Factorio(바이터), Mindustry (선택적, 권장하지 않음)
- 이 게임은 전투가 설계 범위에 없고, 긴장감은 이미 "전력 부족 시 라인 전체 감속"(섹션 1.3-4)으로 확보하고 있음. 적 웨이브형 전투 콘텐츠는 컨셉과 충돌할 수 있어 그대로 가져오지 않는 것을 권장.
- 대신 비전투 이벤트(정전 스톰, 매장지 고갈 경고 등) 정도만 도전 요소로 고려.

### 6.8 UI/QoL — 참고: Factorio(생산 통계 그래프), Satisfactory
- **생산 통계 그래프 (채택)**: 자원별 생산량/소비량 시계열 그래프. Factorio에서 병목 파악의 핵심 도구였던 기능이라 우선순위가 높음.
- **초당 생산량 표시 (채택)**: 그래프와는 별개로, 자원/건물별 현재 순간 처리량("12/s" 형태)을 StatusBar/InspectorPanel에 실시간 표시. 그래프가 "추세"를 보여준다면 이건 "지금 이 라인이 제대로 도는지"를 즉시 확인시켜주는 용도.
- **경고/알림 시스템**: 막힘/정전 등을 MiniLog + 아이콘으로 알림.
- **미니맵**: 청크 기반 무한 월드가 커질수록 미니맵 없이는 탐색이 어려워짐.

### 6.9 도입 우선순위 제안
현재 아키텍처(파일 17개, `Building.js` 1501줄 등) 기준으로 구조 변경이 작고 핵심 재미를 바로 강화하는 것부터 제안한다.

| 우선순위 | 항목 | 상태 | 사유 |
|---|---|---|---|
| **0순위 (확정)** | 생산 통계 그래프 (6.8) | 채택 | 새 엔티티 없이 기존 System 데이터만 시각화, 즉시 체감 |
| **0순위 (확정)** | 초당 생산량 표시 (6.8) | 채택 | StatusBar/InspectorPanel 확장 수준, 구조 변경 최소 |
| **0순위 (확정)** | 블루프린트 복사/붙여넣기 (6.2) | 채택 | "더 크게" 짓기 시작하면 가장 먼저 아쉬워지는 기능 |
| 1순위 | 연구 해금형 채굴기 등급 (6.1) | 채택(설계만, 착수 전) | 매장지 대신 채택된 대안, 콘텐츠 확장의 핵심 축 |
| 2순위 | 컨베이어 속도 티어 (6.2) | 후보 | 기존 구조 재사용, 콘텐츠 볼륨만 늘리면 됨 |
| 2순위 | 다중 발전원 + 배터리 (6.4) | 후보 | 신규 건물 몇 개 + 기존 PowerSystem 확장선 |
| 2순위 | 마일스톤/목표 시스템 (6.6) | 후보 | 방향성 제공, UI 위주 작업 |
| 3순위 | 액체/파이프라인 (6.1) | 후보 | 신규 엔티티 타입 + 렌더링 필요, 규모 큼 |
| 3순위 | 장거리 물류(기차) (6.2) | 후보 | 광물 등급 확장 이후 필요성이 커지는 후속 항목 |
| 3순위 | 다단계 정련 트리 확장 (6.3) | 후보 | 콘텐츠는 쉽지만 밸런싱 부담이 큼 |
| 반려 | 매장지(vein)/순도 (6.1) | 반려 | v1.4: 위치 기반 자원 관리가 플레이를 혼란스럽게 함 |

---

## 진행 방식 안내
1. 각 Phase 시작 전, **구현 계획**(생성/수정할 파일, 이유, 예상 클래스 구조)을 먼저 제시합니다.
2. 구현 후 **CHANGELOG** 형식으로 변경 사항을 요약합니다.
3. 각 Phase는 TODO 없이 해당 범위 내에서 완결된 상태로 마무리합니다.
4. 매직 넘버는 전부 `config/`로 분리하고, 하드코딩을 지양합니다.

---

**다음 단계**: 위 설계에 대해 검토/수정 의견을 주시거나, 그대로 진행해도 좋다면 **"다음"**이라고 입력해 주세요. Phase 0부터 구현 계획 제시 → 구현 순으로 시작하겠습니다.